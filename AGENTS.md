# AI Scenery

- 默认使用中文；项目定位是 AI 与 AI Agent 后端应用学习笔记和 Demo。
- Python 使用 `uv` 与项目 `.venv`；不要使用全局 `pip install`。
- Web 框架：Python 用 Flask，Go 用 Gin，Node.js 用 Express。
- 真实密钥只放 `.env.local`，不得读取、打印或提交。
- Demo 保持 API、样例、测试与复盘同步；先运行对应测试再说明结果。
- 关键边界保留简短注释；简单代码不重复解释。
- 不改动本机已有开发环境；确有需要时先征得用户确认。
