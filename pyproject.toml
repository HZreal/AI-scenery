[project]
name = "ai-scenery"
version = "0.1.0"
description = "Personal AI and AI agent backend learning notes and demos"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "flask-smorest>=0.47.0",
]
